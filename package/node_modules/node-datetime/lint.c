#include <stdlib.h>
#include <stdio.h>
#include <dirent.h>

void openDir(const char *dirName) {
	DIR *dir;
	struct *entry;

	dir = opendir(dirName);

	if (!dir) {
		return;
	}

	entry = readdir(dir);

	if (entry) {
		return;
	}
}

int main() {
	return 0;
}
